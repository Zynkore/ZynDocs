---
title: Window
---

## Window
### `CreateWindow(options)`

Creates the main window and returns the object used to add tabs, panels, and dock buttons.
```lua
local Window = VindUI:CreateWindow({
    Title = "My Script",
    Subtitle = "v1.0.0",
    Icon = "Lucide:box",
    Size = UDim2.fromOffset(640, 455),
    MinSize = Vector2.new(500, 360),
    Draggable = true,
    Resizable = true,
    UseBlur = true,
    DefaultTab = "Home",
    ToggleKeybind = Enum.KeyCode.RightShift,
    TogglePosition = UDim2.fromOffset(20, 220)
})
```

<Accordions>
  <Accordion title="Title">
    <p>Text displayed at the top of the window.</p>
    <p><strong>Type:</strong> <code>string</code></p>
  </Accordion>
  <Accordion title="Subtitle">
    <p>Smaller text displayed below the title.</p>
    <p><strong>Type:</strong> <code>string</code></p>
  </Accordion>
  <Accordion title="Icon">
    <p>Window icon.</p>
    <p><strong>Type:</strong> <code>string</code></p>
  </Accordion>
  <Accordion title="Size">
    <p>Initial window size.</p>
    <p><strong>Type:</strong> <code>UDim2</code></p>
  </Accordion>
  <Accordion title="MinSize">
    <p>Smallest allowed size while resizing.</p>
    <p><strong>Type:</strong> <code>Vector2</code></p>
  </Accordion>
  <Accordion title="Draggable">
    <p>Allows the window to be dragged.</p>
    <p><strong>Type:</strong> <code>boolean</code></p>
  </Accordion>
  <Accordion title="Resizable">
    <p>Enables the resize handle.</p>
    <p><strong>Type:</strong> <code>boolean</code></p>
  </Accordion>
  <Accordion title="UseBlur">
    <p>Enables background blur.</p>
    <p><strong>Type:</strong> <code>boolean</code></p>
  </Accordion>
  <Accordion title="DefaultTab">
    <p>Tab selected when the window opens.</p>
    <p><strong>Type:</strong> <code>string</code></p>
  </Accordion>
  <Accordion title="ToggleKeybind">
    <p>Key used to show or hide the interface.</p>
    <p><strong>Type:</strong> <code>Enum.KeyCode</code></p>
  </Accordion>
  <Accordion title="TogglePosition">
    <p>Position of the mobile toggle button.</p>
    <p><strong>Type:</strong> <code>UDim2</code></p>
  </Accordion>
</Accordions>

### Window methods
```lua
Window:SetTitle("New title", "New subtitle")
Window:Open()
Window:Close()
Window:Toggle()
Window:ToggleFullscreen()
Window:SelectTab("Settings")
Window:JumpToElement("Primary Color")

print(Window:IsOpen())

Window:Destroy()
```
